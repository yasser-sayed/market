"use client";

import {
  ChakraProvider,
  Alert,
  AlertIcon,
  AlertTitle,
  AlertDescription,
} from "@chakra-ui/react";

export { ChakraProvider, Alert, AlertIcon, AlertTitle, AlertDescription };
