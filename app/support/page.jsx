import React from "react";
import SupportCC from "./SupportCC";

export const metadata = {
  title: "rsa support",
  description: "contact with rsa developer",
};

const page = () => {
  return (
    <div className="min-h-[80vh]">
      <SupportCC />
    </div>
  );
};

export default page;
